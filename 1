#include <stdio.h>

int max(int a, int b) {
    return (a > b) ? a : b;
}

int main() {
    int num1 = 10;
    int num2 = 25;
    printf("Максимум из %d и %d: %d\n", num1, num2, max(num1, num2));
    return 0;
}
